services:
  mreys-net:
    build: 
      dockerfile: Dockerfile
      context: .
    container_name: mreys-net
    restart: always
    volumes:
      - ./src:/app
      - /app/node_modules
    networks:
      - nginx_proxy-network

networks:
  nginx_proxy-network:
    external: true